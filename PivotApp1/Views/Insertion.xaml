<phone:PhoneApplicationPage
    x:Class="PivotApp1.Contents.Insertion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="MY APPLICATION" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="Insertion" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0">
            <Image HorizontalAlignment="Left" Height="185" Margin="113,10,0,0" VerticalAlignment="Top" Width="205" Name="Image" Tap="Image_Tap" />
            <TextBox HorizontalAlignment="Left" Height="72" Margin="88,200,0,0" TextWrapping="Wrap" Name="Name" Text="" VerticalAlignment="Top" Width="325"/>
            <toolkit:ListPicker x:Name="Option1" Header="Kind" Margin="15,280,15,0">
                <toolkit:ListPickerItem x:Name="Trousers" Content="Trousers"/>
                <toolkit:ListPickerItem x:Name="TShirt" Content="T-Shirt"/>
                <toolkit:ListPickerItem x:Name="Coat" Content="Coat"/>
                <toolkit:ListPickerItem x:Name="Shoes" Content="Shoes"/>
            </toolkit:ListPicker>
            <toolkit:ListPicker x:Name="Option2" Header="Type" Margin="15,380,15,0">
                <toolkit:ListPickerItem x:Name="Cold" Content="Cold"/>
                <toolkit:ListPickerItem x:Name="Warm" Content="Warm"/>
                <toolkit:ListPickerItem x:Name="Rainy" Content="Rainy"/>
            </toolkit:ListPicker>
            <Button Content="Add" HorizontalAlignment="Left" Margin="178,525,0,0" VerticalAlignment="Top" Name="AddButton" Click="AddButton_Click"/>
            <TextBlock HorizontalAlignment="Left" Margin="15,225,0,0" TextWrapping="Wrap" Text="Name:" VerticalAlignment="Top"/>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>